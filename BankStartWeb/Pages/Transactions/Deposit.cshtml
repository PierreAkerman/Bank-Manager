@page
@model BankStartWeb.Pages.Transactions.DepositModel
@{
}


<section class="contact-us section bg-gray" id="contact">
    <div class="container">
        <div class="row">
            <div class="col">
                <div class="title text-center">
                    <h4>Operation</h4>
                    <h2>Make Deposit</h2>
                    <span class="border"></span>
                    <h5 class="text-info">@Model.Fullname</h5>
                    <h5 class="text-info">@Model.AccountType Account</h5>
                    <h5 class="text-info">@Model.Balance $</h5>
                    <span class="border"></span>
                    <p>Enter the amount to deposit on this account in the input field below</p>
                </div>
            </div>
        </div>
        <div class="row">
            <!-- Contact Form -->
            <div class="contact-form col-12 col-md-6  mx-auto" >
                <form id="contact-form" method="post">
                    <div class="form-group">
                        <input asp-for="Amount" class="form-control">
                        <span asp-validation-for="Amount" class="field-validation-error"></span>
                    </div>
                    <div>
                        <input type="submit" id="contact-submit" class="btn btn-transparent mb-1" value="Make Deposit">

                    </div>						
                </form>
                <a class="btn" id="contact-submit"  asp-page="/Accounts/AccountDetails" asp-route-accountid="@Model.Id">Back to @Model.AccountType Account</a>
            </div>
            <!-- ./End Contact Form -->
        </div> <!-- end row -->
			
    </div> <!-- end container -->
</section> <!-- end section -->


@*<div class="card">
    <div class="card-header">
        <h1>Deposit</h1>
    </div>
    <div class="container">
        <ul>
            <li>Name: @Model.Fullname</li>
            <li>Balance: @Model.Balance</li>
            <li>@Model.AccountType Account</li>
        </ul>
        <form method="post" class="form-control">
            <div class="form-group">
                <label class="form-control" asp-for="Amount">Amount</label>
                <input class="form-control" asp-for="Amount">
                <span asp-validation-for="Amount" class="field-validation-error"></span>
            </div>
            <button type="submit" class="btn">Make Deposit</button>
        </form>
    </div>
</div>*@